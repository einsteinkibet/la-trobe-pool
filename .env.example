# Frontend (Vite) environment — copy to .env for production builds.

# The deployed backend API base URL, including the /api suffix.
# Leave UNSET for local dev (Vite proxies /api to the local backend).
VITE_API_URL=https://your-backend-domain.com/api
